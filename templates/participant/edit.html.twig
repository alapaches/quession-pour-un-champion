{% extends 'base.html.twig' %}

{% block title %}Modifier un participant{% endblock %}

{% block body %}
    <h1 class="center page-title">Modifier un participant</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_participant_index') }}">back to list</a>

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
