{% extends 'base.html.twig' %}

{% block title %}Liste des jeux{% endblock %}

{% block body %}
    <h1>Liste des jeux</h1>

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for jeux in jeuxes %}
            <tr>
                <td>{{ jeux.nom }}</td>
                <td>
                    <a class="btn btn-xs btn-secondary" role="button" href="{{ path('app_jeux_show', {'id': equipe.id}) }}">
                        <i class="fa-solid fa-eye"></i> Voir
                    </a>
                    <a class="btn btn-xs btn-warning" role="button" href="{{ path('app_jeux_edit', {'id': equipe.id}) }}">
                        <i class="fa-solid fa-pen-to-square"></i> Modifier
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td class="center" colspan="2">Aucune donnée n'est disponible</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-xs btn-success" role="button" href="{{ path('app_jeux_new') }}">
        <i class="fa-solid fa-plus"></i> Ajouter un jeu
    </a>
{% endblock %}
