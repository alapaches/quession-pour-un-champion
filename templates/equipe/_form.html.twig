{{ form_start(form) }}
    {{ form_widget(form) }}
    <a class="btn btn-xs btn-warning" href="{{ path('app_equipe_index') }}">Retour</a>
    <button class="btn btn-xs btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
