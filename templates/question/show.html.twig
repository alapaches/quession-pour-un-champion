{% extends 'base.html.twig' %}

{% block title %}Question: {{question.intitule}}{% endblock %}

{% block body %}
    <h1 class="center page-title">Question: {{question.intitule}}</h1>
    <div class="center">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th class="center">Intitule</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td class="center">{{ question.intitule }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-xs btn-secondary btn-retour" role="button" href="{{ path('app_question_index') }}">
            <i class="fa-solid fa-left-long"></i> Retour à la liste
        </a>
        <a class="btn btn-xs btn-warning" role="button" href="{{ path('app_question_edit', {'id': question.id}) }}">
            <i class="fa-solid fa-pen-to-square"></i> Modifier
        </a>
        {{ include('question/_delete_form.html.twig') }}
    </div>
{% endblock %}
