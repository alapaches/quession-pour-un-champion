{% extends 'base.html.twig' %}

{% block title %}Modifier une proposition{% endblock %}

{% block body %}
    <h1>Modifier une proposition</h1>

    {{ include('proposition/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {# <a href="{{ path('app_proposition_index') }}">back to list</a> #}

    {{ include('proposition/_delete_form.html.twig') }}
{% endblock %}
